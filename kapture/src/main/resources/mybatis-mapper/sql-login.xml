<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.kapture.login.mapper.LoginMapper">
	<select id="getlogin" parameterType="hashmap" resultType="com.example.kapture.login.model.Login">
		SELECT *
		FROM USERS 
		WHERE EMAIL = #{email} AND
		PASSWORD = #{password}  
	</select>
	
	<insert id="insertUser" parameterType="hashmap">
		INSERT INTO USERS (USER_NO, PASSWORD, EMAIL, PHONE, ROLE, BIRTHDAY, U_CREATED_AT, U_UPDATED_AT, USER_FIRSTNAME, USER_LASTNAME)
		VALUES (USERS_SEQ.NEXTVAL, #{password}, #{email}, #{phone}, 'TOURIST', #{birthday, jdbcType=DATE}, SYSDATE, SYSDATE,
		 #{firstName}, #{lastName})
	</insert>
	
	<select id="checkUser" parameterType="hashmap" resultType="com.example.kapture.login.model.Login">
		SELECT *
		FROM USERS 
		WHERE EMAIL = #{email}
		
	</select>
	
	<select id="selectUserByEmail" parameterType="string" resultType="map">
    SELECT user_no AS userNo,
           email AS userEmail,
           user_firstname AS userFirstName,
           user_lastname AS userLastName
    FROM users
    WHERE email = #{email}
	</select>

	<insert id="insertSocialUser" parameterType="map">
        INSERT INTO users(
            email,
         	password,
            user_firstname,
            user_lastname,
            social_type
        ) VALUES (
            #{userEmail},
            'Test1234!'
            #{userFirstName},
            #{userLastName},
            #{socialType}
        )
    </insert>

</mapper>




