<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.kapture.login.mapper.LoginMapper">
	<select id="getlogin" parameterType="hashmap" resultType="com.example.kapture.login.model.Login">
		SELECT *
		FROM USERS 
		WHERE EMAIL = #{email} AND
		PASSWORD = #{password}  
	</select>
	
	<insert id="insertUser" parameterType="hashmap">
		INSERT INTO USERS (USER_NO, PASSWORD, EMAIL, PHONE, ROLE, BIRTHDAY, CREATED_AT, UPDATED_AT, USER_FIRSTNAME, USER_LASTNAME)
		VALUES (USERS_SEQ.NEXTVAL, #{password}, #{email}, #{phone}, 'TOURIST', #{birthday},
		SYSDATE, SYSDATE, #{firstName}, #{lastName})
	</insert>

</mapper>




